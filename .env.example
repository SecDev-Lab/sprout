# Example environment configuration
API_KEY={{ API_KEY }}
WEB_PORT={{ auto_port() }}
DB_PORT={{ auto_port() }}
REDIS_PORT={{ auto_port() }}

# Static configuration
NODE_ENV=development
LOG_LEVEL=debug

# Docker Compose variables (preserved)
COMPOSE_PROJECT_NAME=${COMPOSE_PROJECT_NAME:-sprout}